# ─── RedFlag CI Frontend Environment Variables ──────────
# Copy to .env and fill in your values.

# API base URL — point to your deployed backend or local dev server
VITE_API_URL=http://localhost:8000
